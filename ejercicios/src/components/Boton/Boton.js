'use strict';
const ReactElement = React.createElement;
export default class Boton extends React.Component {

    render() {
        return ReactElement(
            'button',
            null,
            'Super Boton'
           
        );
    }
}
